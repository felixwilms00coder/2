{
  "name": "auto-broker-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Self-managed MCP server that connects your own IBKR account to an MCP client. Runs locally, holds only your own credentials, never sends an order without an explicit confirm step.",
  "type": "module",
  "bin": {
    "auto-broker-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "undici": "^7.2.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
