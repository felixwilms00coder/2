# /pilots fetches public 13F filings from SEC EDGAR (data.sec.gov, sec.gov).
# SEC asks for a contact email in the User-Agent of automated requests (see
# sec.gov/os/webmaster-faq#developers). The page still works without this,
# but with a generic User-Agent SEC may rate-limit more aggressively.
SEC_EDGAR_CONTACT_EMAIL=
